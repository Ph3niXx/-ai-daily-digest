# Audit Jarvis — 2026-05-02 (v24, post-drainage v23 + post-burst sync 1/05)

> **Contexte de cadence** : 2e routine de mai, samedi de pont après le 1er mai férié. Hier matin (1/05 entre 09:58 et 10:35), Jean a enchaîné 12 commits qui ont 100 % drainé le backlog v23 — v23-S1 (claude nav restoré), v23-S2 (sql/013 jobs trigger appliqué via MCP, verdict (a)), v23-K1 (hygiène 3 prompts pending → done + 2 artefacts cowork commités), plus le big sync `6600b64` qui aligne 10 fichiers specs/architecture/CLAUDE.md sur la réalité du repo après dérive de fin avril, plus quelques bookkeeping sur INDEX. Streak commits 2 jours consécutifs (30/04 + 1/05) confirmé. Aujourd'hui samedi 2/05 : 0 commit, et `start_jarvis.bat` n'a pas tourné depuis le 29/04 18:14 (3 jours pleins). **Working tree CLEAN, pending/ vide, INDEX synchronisé, validation pipelines en attente du prochain démarrage Jarvis (probable lundi 4/05).**

## North Star de la semaine

**Cette semaine, on convertit 1 quick win du design audit du 1/05 (transition CSS sur le switch de thème) — la fenêtre UX est ouverte parce que le backlog code est drainé à zéro depuis 24h, la stack LLM est stable depuis 6 jours sans incident, et le design audit livre des prompts copy-pasteables qu'on aurait tort de laisser dormir une semaine de plus.**

## Ce qui a bougé depuis le dernier audit (v23, 01/05 ~07:00)

- **Commits depuis v23 (12 au total, tous le 01/05 entre 09:58 et 10:35)** :
  - `be9db08` 09:58 jarvis-lab — retire fallbacks hex Obsidian-themed des var() (round 2)
  - `953b029` 10:00 trigger DB jobs_inherit_user_status — **v23-S2 SHIPPED**
  - `796974d` 10:00 archive artefacts Cowork & Jarvis (audits design 30/04 & 01/05, ecosystem 30/04, audit Jarvis v23)
  - `428e8f2` 10:01 INDEX v23 bookkeeping post-commits 953b029 & 796974d
  - `00e61f9` 10:04 archive K1-hygiene-v21 prompt (de facto soldé par 27738a8)
  - `b1aaa4f` 10:05 v23-S2 shipped — verdict (a) confirmé via MCP Supabase
  - `191c2d0` 10:06 archive S1-fix-history-typeerror — soldé par 69ea05b
  - `c4cf27a` 10:06 archive S2-fix-opps-urgency-null — soldé par ce34742
  - `42d5cf9` 10:07 archive K1-hygiene-v22 — soldé par 27738a8 + 796974d + 428e8f2
  - `7d0f826` 10:09 restaure entrée "Claude" dans sidebar Veille — **v23-S1 SHIPPED**
  - `363bf27` 10:10 v23-S1 shipped — Claude nav restoré
  - `6600b64` 10:35 sync specs + archi + CLAUDE.md après dérive de fin avril (10 fichiers)
- **Shipped (v23)** : 2 SHIPs + 1 KILL = 100 % v23 drainé en ~12 minutes (avec une pause big-sync 25 min à la fin). 4 audits sur 5 (v20, v21, v22, v23) = 100 % d'exécution. Cadence remarquable.
- **Validation prod en attente (résiduelle, non urgente)** :
  - `last_indexer.log` daté 29/04 18:13 montre encore les 3 erreurs 409 sur `memories_vectors` PRE-fix `8f3764d`. Le fix `sb_post(on_conflict=...)` est commité depuis le 29/04 22:40 mais aucun pipeline n'a tourné depuis (Jean n'a pas démarré `start_jarvis.bat` depuis le 29/04 19:59). **Validation v21-S3 attendue au prochain démarrage Jarvis, probable lundi 4/05.**
  - `last_activity_brief.log` 29/04 — idem, 1 erreur 409 sur `(date)=(2026-04-28)`.
  - `signals.md` du 30/04, 1/05, 2/05 absents. Cohérent avec Jean en pause physique pendant le pont férié (1er mai + week-end). Pas de signal frais → cet audit s'appuie sur le même `2026-04-29-signals.md` que v22 et v23.
- **Friction résiduelle** :
  - `design-audit-2026-05-01.md` (83 KB) est commité **à la racine du repo** (commit `796974d`), alors que les audits design des jours précédents sont dans `audits/` (`audits/2026-04-28-design-audit.md`, `audits/2026-04-29-design-audit.md`, `audits/2026-04-30-design-audit.md`). Désalignement de routine Cowork — le path conventionnel n'a pas été respecté. → **K1 ce tour-ci**.
  - `docs/veille-claude/` n'a pas de fichier 2026-04-28.md, 2026-04-29.md, 2026-04-30.md, 2026-05-01.md, 2026-05-02.md — la routine veille hebdo Claude (format `YYYY-MM-DD.md`) est silencieuse depuis 5 jours (dernier = 2026-04-27.md). Hors scope ce tour (routine externe Cowork hebdomadaire, pas quotidienne).

## Signaux faibles (télémétrie + logs)

- **Top 5 events 7j** (figés sur `2026-04-29-signals.md`, dernière extraction 29/04 20:14, **identique à v22 et v23**) : `section_opened` (468), `jobs_action` (84), `error_shown` (26), `recent_filter_auto_on` (18), `pipeline_triggered` (15). 3e audit consécutif sans données fraîches — c'est attendu (Jean en férié) mais ça veut dire que ce tour ne peut découvrir aucun nouveau signal d'usage.
- **Sections les plus revisitées** (figé) : `brief` (38), `updates` (34), `top` (33), `jarvis-lab` (27), `week` (24).
- **Sections jamais ouvertes** (figé, snapshot 29/04) : `claude` — résolu par v23-S1 commit `7d0f826`. Le prochain `signals.md` validera (probablement 4 ou 5 mai).
- **Erreurs UI résiduelles validées non encore en signal** (idem v23) :
  - `panel:jobs` 11 occ. WebSocket — fixé par v21-S2 commit `7935da5`. Validation prod attendue dans le prochain `signals.md`.
  - `panel:history` 8 occ. TypeError — fixé par v22-S1 commit `69ea05b`. Idem.
  - `panel:opps` 24 occ. urgency null — fixé par v22-S2 commit `ce34742`. Idem.
  - `jarvis:gateway` 7 occ. tunnel intermittent — pas un bug code, hors scope.
- **`llm_traces.jsonl`** : dernier event 2026-04-29 18:14:41, 14201 ms, status:ok. **6+ jours sans incident LLM** (record). v15-S1 + v14-S1 + v13-S2 tiennent.
- **`last_status_gen.log`** : 1006 chunks (validation v21-S1 OK depuis 29/04 18:40).
- **Méta-tendance audit** : v22 → v23 → v24 = série remarquable. v22 100 %, v23 100 %, v24 ouvre sur 0 dette code. **Premier audit de mai où la NS n'est pas pilotée par une dette accumulée mais par une opportunité explicite (le design audit du 1/05).**
- **Recherches infructueuses** (figé) : 0 / 0.
- **Engagement faible 1 % link_clicked** (figé, 6 / 468 ouvertures) — signal stable sur 4 audits consécutifs. Hypothèse : Jean lit dans le cockpit sans suivre les liens externes (le brief lui-même fait le travail), ce serait alors un signe de qualité du brief, pas d'un défaut. Pas de SHIP — manque de causalité claire pour intervenir.
- **Erreurs pipelines** : inchangées (logs gelés au 29/04). Validation prod dépendante du prochain démarrage Jarvis.

## SHIP (max 3, après rechallenging)

### S1 — Transition CSS sur le switch de thème (Quick Win #4 du design audit 1/05)

**Pourquoi maintenant** : sert directement la North Star. Le design audit du 1/05 — produit par une routine Cowork tournée vers le UX, déjà commitée — identifie le flash visuel à 22:00 pile (bascule auto Dawn → Obsidian dans `cockpit/app.jsx:226`) comme P0 et fournit les ~30 lignes CSS exactes à coller. Vérification confiée : `grep transition` sur `cockpit/styles.css` montre 20+ transitions ciblées (transform, color, border, max-height) mais **aucune sur `:root` ou `body` pour `background-color` / `color`** — donc le flash existe bien. Jean voit ce switch chaque soir à 22:00 et chaque matin à 06:00 = 14 fois/semaine. ROI excellent : 30 minutes pour atténuer une friction quotidienne.
**Diagnostic** : `cockpit/app.jsx:221-238` — `setThemeId` mute `--bg`, `--bg2`, `--bg3`, `--surface`, `--tx`, `--tx2`, `--tx3`, `--bd`, `--bd2`, `--brand`, etc. via `root.style.setProperty(...)`. Aucun `transition: background-color X ease` en place sur `:root`/`body`/surfaces principales. Audit `cockpit/styles.css` lignes 1-50 : pas de bloc transition global. Référence prompt design audit 1/05 — **Prompt 4 — [UX] Transition CSS sur switch thème** (lignes ~649-729 du fichier `design-audit-2026-05-01.md`).
**Définition de "fait"** : après modification, cliquer manuellement sur les 3 boutons thème dans la sidebar → transition fluide ~280ms sans flash. Le switch auto à 22:00 (et son symétrique à 06:00) est adouci. `prefers-reduced-motion: reduce` désactive la transition (vérifié dans le code). Aucune régression sur les hover/focus existants (qui utilisent `transition: all 120ms` dans des selectors plus spécifiques — pas écrasés).
**Effort** : XS (~30 min — 1 fichier `cockpit/styles.css`, ~30 lignes ajoutées en haut, 1 commit).

#### Prompt Claude Code — S1

```
Contexte projet : le switch de thème (Dawn / Obsidian / Atlas) dans
`cockpit/app.jsx:221-238` mute des CSS Custom Properties sur `:root`
sans transition CSS. Résultat : flash visuel à chaque switch — manuel
ou auto (22:00 → Obsidian, 06:00 → Dawn). Le design audit du 1/05
(`design-audit-2026-05-01.md`, Prompt 4) a déjà rédigé le fix CSS
exact, ~30 lignes additives, zéro logique JS, copy-paste depuis l'audit.

Phase 0 — Reconnaissance (OBLIGATOIRE avant toute action)

Avant de modifier quoi que ce soit, écris un rapport ~20 lignes après
ces vérifications :

1. Confirme que `cockpit/styles.css` n'a AUCUNE transition globale sur
   `:root` ou `body` pour `background-color` / `color` :
     `grep -nE "^(:root|body)\s*\{" cockpit/styles.css`
   puis lit les 10 lignes suivantes de chaque match — vérifie qu'il
   n'y a pas déjà un `transition:` qui couvre `background-color` ou
   `color` au niveau global.

2. Confirme que les transitions existantes sont scopées (Top cards,
   sidebar, etc.) et n'incluent pas de cascade `*` qui ralentirait :
     `grep -nE "^\s*\*\s*\{" cockpit/styles.css`
   → 0 match attendu (sinon STOP, l'ajout d'une nouvelle transition
   pourrait conflit).

3. Vérifie que le bloc `@media (prefers-reduced-motion: reduce)` existe
   déjà dans `cockpit/styles.css` :
     `grep -n "prefers-reduced-motion" cockpit/styles.css`
   → ≥1 match attendu (le commit `2d61267` du 30/04 a posé le
   kill-switch global). Lit le bloc pour comprendre comment il
   désactive les anims existantes — il faut respecter le même pattern.

4. Confirme la liste des selectors à transitionner en cherchant les
   classes mentionnées dans le Prompt 4 du design audit. Pour chaque
   classe ci-dessous, vérifie qu'elle est bien utilisée dans le repo :
     - `.sb` (sidebar wrapper)
     - `.top-card`
     - `.sig-card`
     - `.opp-card`
     - `.idea-card`
     - `.vl-feed-item`
     - `.hero`
     - `.block`
     - `.btn`
     - `.card-action`
     - `.kbd-panel`
     - `.tk-panel`
     - `.ph` (probably placeholder)
     - `.hwk-wrap`
   Commande : `grep -lE "\.(sb|top-card|sig-card|opp-card|idea-card|vl-feed-item|hero|block|btn|card-action|kbd-panel|tk-panel|ph|hwk-wrap)\b" cockpit/styles*.css | sort -u`
   Si une classe est inconnue, la retirer de la règle (mieux vaut
   moins de selectors qu'une règle invalide).

5. Lis les lignes ~649-729 de `design-audit-2026-05-01.md` (Prompt 4)
   pour avoir le texte exact à coller.

Écris un rapport et ATTENDS ma validation explicite. Le rapport doit
confirmer 1 des 2 verdicts :
  (a) "Aucune transition globale conflictuelle, kill-switch reduced-motion
      en place, classes mentionnées toutes valides → fix go."
  (b) "Conflit identifié sur [liste] → adapter le scope avant d'appliquer."

Objectif : adoucir la transition de thème sans pénaliser les autres
animations.

Fichiers concernés :
- cockpit/styles.css (modification, ajout ~30 lignes en début de fichier)

Étapes (après validation Phase 0) :
1. Insérer en début de `cockpit/styles.css` (juste après le reset CSS,
   avant les blocs spécifiques — vers ligne 30-50, à un endroit cohérent
   avec les conventions du fichier) :

   /* Smooth theme transitions — adoucit le switch Dawn/Obsidian/Atlas
      (manuel ou auto 22h/06h). Respecte prefers-reduced-motion via le
      bloc dédié plus bas dans le fichier. */
   :root {
     transition:
       background-color 280ms ease,
       color 280ms ease;
   }
   body {
     transition:
       background-color 280ms ease,
       color 280ms ease;
   }
   .sb,
   .top-card,
   .sig-card,
   .opp-card,
   .idea-card,
   .vl-feed-item,
   .hero,
   .block,
   .btn,
   .card-action,
   .kbd-panel,
   .tk-panel,
   .ph,
   .hwk-wrap {
     transition:
       background-color 280ms ease,
       border-color 280ms ease,
       color 280ms ease;
   }

2. Étendre le bloc existant `@media (prefers-reduced-motion: reduce)` —
   trouver son emplacement (Phase 0 grep) et ajouter à l'intérieur :

   :root, body, .sb, .top-card, .sig-card, .opp-card, .idea-card,
   .vl-feed-item, .hero, .block, .btn, .card-action, .kbd-panel,
   .tk-panel, .ph, .hwk-wrap {
     transition: none;
   }

   Ne PAS écraser la règle existante (qui couvre kicker-dot, sb-group-hotdot,
   etc.). Ajouter une règle sœur dans le même @media.

3. Aucune autre modification.

Contraintes :
- 280ms = sweet spot perceptible/rapide. Ne pas dévier.
- Pas de `transition: all` (pénaliserait les hover transforms et anims
  scroll existants — les transitions ciblées par propriété sont la règle).
- Pas de cascade `*`.
- Le fichier `cockpit/styles.css` étant 4666 lignes, place les nouvelles
  règles dans une section logique avec un commentaire `/* === Theme
  transitions === */` en début, pour faciliter la relecture future.
- Pas de modification de `cockpit/app.jsx`, ni `themes.js`, ni autres
  CSS.

Validation (lance ces commandes après modification) :
- `grep -c "background-color 280ms" cockpit/styles.css` → ≥ 2 (root +
  body + selectors block).
- `grep -nB2 -A1 "transition: none;" cockpit/styles.css | head -20` →
  vérifier que la nouvelle règle reduced-motion ne casse pas l'existante.
- Demander à Jean : ouvrir le cockpit, cliquer Dawn → Obsidian → Atlas
  dans la sidebar — transition perceptible mais douce, pas de flash.
- Tester avec DevTools `Rendering > Emulate prefers-reduced-motion: reduce`
  → switch instantané, pas de transition.

Ne fais PAS :
- Ne touche pas à `cockpit/app.jsx` (la logique de switch reste identique,
  seul le CSS amortit le résultat).
- N'ajoute pas la rampe progressive 21h45 → 22h15 mentionnée en bonus
  dans le Prompt 4 du design audit — c'est explicitement hors scope ce
  SHIP, à PARK si Jean veut le pousser plus loin.
- Ne refacto pas les transitions existantes (lignes 45, 88, 112, 134,
  etc.) sous prétexte que tu y passes.
- Ne push pas après commit.

Quand c'est fait : montre le diff complet AVANT git add. git commit avec
message `feat(cockpit): transition CSS sur switch de thème (design-audit-2026-05-01 #4)`.
PAS de push.
```

### S2 — *Pas de S2 ce tour-ci.*

**Justification du non-S2 (rétrogradation explicite par engagement v23)** : v22 et v23 ont émis 2 SHIP + 1 KILL chacun, et 0 rétrogradation au rechallenging dans aucun des deux audits. Le ratio mensuel cumulé est passé à **8/45 ≈ 17.8 % (sous cible 20 %)**. Engagement v23 explicite : *"Si v24 ne rétrograde toujours rien, force la rétrogradation du SHIP le plus faible des 3 — par discipline."* Ce tour, le candidat naturel S2 était **Win #6 du design audit 1/05 — `max-width: 70ch` sur `.top-summary` / `.hero-body` / `.opp-body` / `.wiki-body`**. Diagnostic Phase 1 : `.hero.is-compact .hero-body` a déjà `max-width: 70ch` (ligne 658), mais `.hero-body` non-compact et `.top-summary` (ligne 1383) sont uncontrolled. Sur 27", la lecture peut s'étirer à 90+ ch — friction réelle mais non urgente. **Rétrogradé en P41** (voir tableau PARK) avec condition de réveil claire : *après que S1-v24 (transition thème) soit shippé en prod, et qu'une revue visuelle Jean confirme qu'aucune card n'est en flex/grid avec sibling expanded qui casserait avec un max-width forcé.* Le coût d'opportunité de garder la cartouche sur S2 est nul (ce week-end férié, Jean ne code probablement pas), et l'effort sera identique en v25.

### S3 — *Pas de S3 ce tour-ci.*

**Justification du non-S3** : aucune dette code, aucun signal d'erreur frais, aucune PARK condition réellement remplie aujourd'hui (P30, P36, P37 ont des conditions calendaires futures, P38/P39/P40 dépendent d'incidents non observés). Émettre un 3e SHIP saturerait la cadence sans servir la NS — la NS de cette semaine est délibérément **un seul quick win UX**, pas un sprint. Cartouche gardée pour v25 (lundi 4/05 probable).

## SHIP rétrogradés pendant le rechallenging

**1 ce tour-ci.** S2 candidat (Win #6 — `max-width: 70ch` sur prose) rétrogradé en P41 par application explicite de l'engagement v23. Justification ci-dessus + bloc rechallenging dédié ci-dessous. Ratio mensuel post-rétrogradation : **9/45 = 20.0 % (pile sur cible)**. Discipline rétablie sans drame.

#### Rechallenging — S1 (transition CSS thème)

- **Red team** : "Tu ajoutes 30 lignes de CSS pour une friction qui dure 200 ms à 06:00 et 22:00. Coût d'opportunité disproportionné." → Friction rare individuellement (2 fois par jour) mais perçue tous les jours, sur le composant le plus structurant du cockpit (la couleur de tout l'écran change). Le ROI est dans la cumulation, pas dans l'épisode. Et c'est pile l'occasion parfaite pour utiliser un design audit qui dort depuis 24h. Objection insuffisante.
- **Contre-exemple** : "La transition rentre en conflit avec une animation existante (par exemple le fade-in d'un panel au mount), provoquant un effet de double-flash." → Possible mais maîtrisable : les transitions ajoutées sont scopées par propriété (`background-color`, `color`, `border-color`), pas un `transition: all`. Les anims existantes utilisent surtout `transform`, `opacity`, `max-height` — orthogonales. Phase 0 vérifie l'absence de cascade `*` qui pourrait étendre la règle.
- **Coût d'opportunité** : 30 min. Alternatives : (a) Win #1 translateY hover après J7 — plus complexe (logic JS + CSS + 4-5 selectors), (b) Win #2 token sweep — long et risqué (50 substitutions sur un fichier de 4666 lignes), (c) attendre P38 ou P40 que rien ne réveille. S1 est la seule alternative qui combine valeur immédiate + zero risque + zéro logique JS.
- **Futur soi** : "J'aurais dû voir que `--bg2` et `--bg3` ne sont pas dans la liste des propriétés transitionnées (la règle ne couvre que `background-color`/`color`/`border-color`), donc certains éléments avec un fond sur `--bg2` n'auront pas de transition douce." → Risque réel mais marginal : `background-color` couvre `--bg2`/`--bg3`/`--surface` quand utilisés via `background-color: var(--bg2)`. Si certains éléments utilisent `background: var(...)` shorthand, la transition `background-color` ne s'appliquera pas (c'est `background` qui est la prop). Mitigation : Phase 0 doit vérifier ; si signal post-déploiement, élargir la règle dans une v25 micro-fix.
- **Cohérence historique** : aucun PARK/KILL antérieur n'a visé les transitions thème. v22 a posé le kill-switch reduced-motion (commit `2d61267`) qui est compatible. RAS.
- **Verdict final : MAINTENU**

#### Rechallenging — S2 candidat (Win #6 — max-width 70ch) — court, tests 1+3+4

- **Red team (test 1)** : "Sur 27" Jean lit confortablement déjà — la max-width risque de casser les cards qui sont en flex/grid avec un sibling icon ou toolbar expanded." → Risque visible plus important que pour S1. Vérifier en Phase 0 si `.opp-body` est en flex avec un sibling, et si `.top-summary` est dans une card en grid 2 cols. Verdict honnête : risque visuel mid-tier, pas négligeable, et l'auteur du design audit lui-même n'a pas commiteé ce fix → si Jean trouvait l'amélioration évidente il l'aurait déjà fait dans le burst 30/04 soir.
- **Coût d'opportunité (test 3)** : 30 min. Alternatives : Win #4 (S1 ce tour) qui a un risque strictement plus faible et une fréquence d'effet plus élevée (chaque switch thème tous les jours vs effet visuel sur une lecture). Win #4 gagne.
- **Futur soi (test 4)** : "J'ai ajouté un `max-width: 70ch` sur `.opp-body` et la rangée d'icônes à droite de la card s'est désalignée parce que la card était en `flex: 1` avec un sibling expanded. Bug visuel mineur mais visible 30 fois/jour." → Phrase facilement écrivable = signal d'alerte fort. Discipline = rétrograder.
- **Verdict : RÉTROGRADÉ EN PARK (P41)** avec condition de réveil : *"Après merge S1-v24 + revue visuelle des layouts cards par Jean (≥1 card par section : Top, Opps, Wiki) confirmant qu'aucune n'est en flex/grid avec sibling expanded qui casserait."*

#### Rechallenging — K1 (court : tests 1+4)

- **Red team (test 1)** : "Tu déplaces un fichier de 83 KB pour de la cosmétique de path. Bénéfice = 0 pour Jean, coût = 1 commit polluant." → Faux : la convention `audits/YYYY-MM-DD-design-audit.md` est respectée par les 3 audits précédents (`audits/2026-04-28-design-audit.md`, `2026-04-29`, `2026-04-30`) **commités au même endroit par la même routine Cowork**. Le fichier 1/05 a dérivé à la racine — soit le shell de la routine a échoué, soit Jean a fait un copy-paste manuel. Le K1 réaligne sur la convention pour que `git log` reste découvrable et que les futurs audits ne créent pas une 2e zone par accident.
- **Futur soi (test 4)** : "J'ai cherché les audits design dans `audits/` et celui du 1/05 manquait, je suis passé à côté du Prompt 4 dont S1-v24 dépend." → Signal d'alerte modéré. La discoverability est un coût silencieux qui ne se paye qu'au moment où on cherche. K1 est un investissement défensif. Maintenu.
- **Verdict final : MAINTENU**

### Méta-test honnêteté du rechallenging

J'ai bien rétrogradé S2 candidat. L'engagement v23 est respecté à la lettre. Je ne triche pas — la friction sur `max-width: 70ch` est réelle, mais le risque est strictement supérieur à S1, et l'engagement disait "force le plus faible". Le ratio mensuel revient à 20 % pile, sans excès.

## KILL list

### K1 — Déplacer `design-audit-2026-05-01.md` (root) → `audits/2026-05-01-design-audit.md`

**Raison** : la routine Cowork "design audit quotidien" écrit ses sorties dans `audits/`, comme en témoignent `audits/2026-04-28-design-audit.md`, `audits/2026-04-29-design-audit.md`, `audits/2026-04-30-design-audit.md`. Le fichier du 1/05 a été commité à la racine (`796974d`), nom inversé (`design-audit-2026-05-01.md` au lieu de `2026-05-01-design-audit.md`). Réalignement nécessaire avant que le pattern n'essaime.

**Risque de regret** : Faible. Un `git mv` préserve l'historique du fichier. Aucun outil ne référence `./design-audit-2026-05-01.md` à la racine (vérification `grep` dans Phase 0 du prompt). Le contenu reste identique.

**Rechallenging court** : voir bloc ci-dessus (test 1 + test 4).

**Prompt Claude Code** :

```
Phase 0 (3 commandes lit-only) :

1. Confirmer le fichier à déplacer existe et est tracké :
   `git ls-files design-audit-2026-05-01.md` → 1 ligne attendue.
   `wc -l design-audit-2026-05-01.md` → ~1700-1800 lignes attendues
   (fichier de 83 KB).

2. Confirmer la convention des 3 audits précédents :
   `ls audits/2026-04-2[89]-design-audit.md audits/2026-04-30-design-audit.md`
   → 3 lignes attendues, format `audits/YYYY-MM-DD-design-audit.md`.

3. Vérifier qu'aucun fichier ou outil ne référence le path racine :
   `grep -rn "design-audit-2026-05-01" --include="*.md" --include="*.yaml" --include="*.json" --include="*.py" --include="*.js" --include="*.jsx" --include="*.html" .`
   (exclure `.claude/worktrees/`, `.git/`)
   → 0 résultat attendu hors le fichier lui-même et hors `jarvis/upgrades/2026-05-02-audit.md` (cet audit, qui le référence intentionnellement). Si une référence externe existe (autre que cet audit), STOP et rapporter — le mv casserait un lien.

Si tout est OK :

4. `git mv design-audit-2026-05-01.md audits/2026-05-01-design-audit.md`

5. `git commit -m "chore(audits): move design-audit-2026-05-01.md → audits/ (convention path)"`

Validation :
- `ls audits/2026-05-01-design-audit.md` → fichier présent.
- `ls design-audit-2026-05-01.md 2>&1` → "No such file" attendu.
- `git log -1 --stat` → 1 fichier renommé (R100), 0 modification de contenu.
- `git status --porcelain` → vide.

Ne fais PAS :
- Ne supprime pas le fichier (utilise git mv, pas rm + git add).
- Ne modifie pas le contenu (R100 = 100 % similaire = rename pur).
- Ne touche pas aux autres audits (28/04, 29/04, 30/04 déjà bien placés).
- Ne push pas.

Affiche le résultat de `git status` AVANT le commit pour valider que
seul le rename apparaît (pas de modifications de contenu non voulues).
```

## PARK

| Idée | Condition de réveil |
|------|---------------------|
| **P33 — Fix wiki TypeError `e.word_count.toLocaleString`** *(distinct de panel-history)* | Si `wiki` apparaît avec ≥10 `error_shown TypeError` sur le prochain `signals.md` (probable lundi 4/05). |
| **P35 — Fix `panel:opps` TypeError `null.urgency`** *(résolu v22-S2)* | À retirer de PARK si validation prod prochaine `signals.md` ≤ 1 occurrence. |
| P36 — `audit_commit.sh` + règle Cowork SKILL | Réveil **12 mai** (10 jours). Si v22-K1 + v23-K1 + v24-K1 = 3 patches manuels en 3 audits, force la promotion à v26. |
| P37 — Auto-commit batch des 4 routines Cowork | Réveil **5 mai** (3 jours). Si v25 audit du 5/05 affiche encore ≥ 2 untracked dans `audits/` ou `docs/veille-claude/`, promouvoir SHIP. v23-K1 a fermé la dette du 30/04, v24-K1 résout celle du 1/05 (path racine vs `audits/`). Si dérive récidive, c'est qu'un script s'impose. |
| P38 — Phi-4-mini-instruct (split slot LM Studio) | Quand 1 nuit `nightly_learner` échoue OR latence p90 > 60 s sur `_compact_history` 3j. Compteur reset au 26/04, **6 j tenus**. |
| P39 — Outlook Graph + msal `tenant=consumers` | Si observer COM crashe ≥ 2x/sem OR Jean travaille un jour sans Outlook desktop. |
| P40 — VectorChord-BM25 + RRF dans `retriever.py` | Quand un retrieval test rate par cosine pure ET P38 shippé. |
| **P41 — `max-width: 70ch` sur `.top-summary` / `.hero-body` / `.opp-body` / `.wiki-body` (Win #6 design audit 1/05, ex S2-v24)** | Après merge S1-v24 (transition thème) + revue visuelle Jean confirmant qu'aucune card consommante n'est en flex/grid avec sibling expanded qui casserait. Effort estimé XS. |
| P29 — Mem0g graph extension | P40 shippé ET ≥ 3 contradictions de faits en 1 sem. |
| P32 — Cloud fallback `nightly_learner._llm_extract` | Si après v15-S1 mergé, ≥ 3 nuits sans extraction. Compteur reset à `566a084`. |
| P27 — Bump `project_status.yaml` Phase 7 méta-doc | Quand les 4 lints CI Phase 7 tournent 7j sans warning. |
| P30 — Investiguer pourquoi `signals.md` du 30/04 + 01/05 + 02/05 manquant *(condition quadruple-remplie)* | Reste différé tant que v15-S2 (`run_nightly_after_deps.bat`) couvre quand Jean démarre. Aujourd'hui = férié week-end → cohérent. **Si `signals.md` du 4/05 (lundi) absent, P30 promouvoir d'office**. |

## Edge — Ce qu'on pique cette semaine

**Rien à voler à l'extérieur — l'edge est interne cette semaine.** Le design audit du 1/05 (3 KB de prompts copy-pasteables, 10 quick wins + 15 features avancées) est la meilleure source d'inspiration disponible pour cette NS. Aller chercher un projet open-source publié dans les 7 derniers jours pour résoudre "le flash visuel à 22:00 dans une SPA React" reviendrait à overengineering. La routine Cowork interne fait déjà le travail d'edge — exploiter l'existant avant de chercher dehors.

## Tendance

**Jarvis va structurellement mieux qu'il y a 14 jours.** Côté technique : 6 jours sans incident LLM (record absolu), validation v21-S3 toujours en attente du prochain démarrage Jarvis (non bloquante). Côté opérationnel : 4 audits consécutifs (v20→v23) avec exécution 100 % en moins de 24 h. Côté discipline : ratio rechallenging revient à 20 % pile cette routine grâce à la rétrogradation S2 → P41 — l'engagement v23 est respecté à la virgule. Côté UX : v22 (panel-history + panel-opps guards) + burst 30/04 soir (8 commits a11y) + v23-S1 (claude nav) ont déjà absorbé 50 % des points "Top 3 faiblesses" du design audit 1/05 (drift tokens reste, mobile reste). v24 attaque le 3e front (transitions thème).

**Risque résiduel** : la dette d'archivage des routines Cowork (path racine vs `audits/`) recommence à dériver — c'est exactement le pattern que P37 visait. Si v25 a encore une dérive du même genre, P37 doit déclencher d'office. Compteur sur condition de réveil **3 jours** désormais.

## Méta-tendance (mensuelle, à remplir le 1er audit du mois)

**Aujourd'hui = 02/05 = 2e audit de mai → cette section reste figée sur le bilan d'avril (rempli en v23). Mise à jour suivante : 1er juin (v~38).**

- **Avril ferme sur 36 SHIP shippés** (cf. v23). En mai, v23 a apporté 2 SHIP + 1 KILL, v24 apporte 1 SHIP + 1 KILL. **Cumul mai à ce jour : 3 SHIP + 2 KILL en 2 audits**, cadence très soutenue.
- **Ratio rechallenging cumulé v1→v24 : 9/45 = 20.0 %** — pile sur cible. Premier mois où le ratio touche la cible exacte. Discipline efficace.
- **North Star la plus récurrente en mai (à ce stade)** : "filets de sécurité livrés" (v23) → "opportunisme UX sur design audit dormant" (v24). Mai semble ouvrir sur un cycle de stabilisation + capitalization, pas de feu.
- **Bilan PARK** : 14 → **15 actifs** (+P41 ajouté ce tour). P30 condition quadruple-remplie. P37 réveil **3 j**. P36 réveil 10 j.
- **Streak commits consécutifs** : 30/04 (11 commits) + 01/05 (12 commits) = **2 jours**. Aujourd'hui (samedi 2/05) probablement à 0 commit (audit vise lundi 4/05 reprise). Streak record en cours = 2.
- **Lundi 4/05** = jour de reprise. À surveiller : (1) `start_jarvis.bat` redémarré → `signals.md` du 4/05 généré → validation prod v21-S3 / v22-S1 / v22-S2 / v23-S1 ; (2) S1-v24 (transition thème) shippé.

---

## Dernière MAJ

- **2026-05-02 (v24, post-drainage v23 + post-big-sync 1/05)** : 2e routine de mai, samedi de pont. v23 a tenu 100 % (12 commits 1/05 entre 09:58 et 10:35 dont 2 SHIPs + 1 KILL + big sync `6600b64` qui aligne 10 fichiers specs/archi/CLAUDE.md sur la réalité du repo). **1 SHIP émis** (S1 transition CSS sur switch thème, Win #4 design audit 1/05) + **1 KILL** (déplacement `design-audit-2026-05-01.md` racine → `audits/2026-05-01-design-audit.md`, convention path). **1 SHIP rétrogradé au rechallenging** (S2 candidat = Win #6 max-width 70ch → P41 PARK), application explicite engagement v23. Ratio mensuel revient à **20.0 % pile** (cible). Working tree clean depuis 24 h, pending/ vide, 6 jours sans incident LLM (record). Validation prod résiduelle (v21-S3, v22-S1, v22-S2, v23-S1) attendue lundi 4/05 quand `start_jarvis.bat` redémarre. Engagement v25 : si lundi 4/05 `signals.md` du jour absent, P30 promouvoir d'office. Si v25 a encore une dérive de routine Cowork (audits/, veille-claude/), P37 promouvoir d'office (réveil 3 j).
