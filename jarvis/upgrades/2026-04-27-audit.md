# Audit Jarvis â€” 2026-04-27

## North Star de la semaine

Cette semaine, le diagnostic Jarvis devient incassable de bout en bout : (a) plus aucun timeout LM Studio ne peut aller Ã  3 attempts Ã— 1114s â€” y compris quand `check_lm_studio()` ne rÃ©pond pas (le filet v14-S1 a un trou qui a dÃ©jÃ  laissÃ© passer 2 cas sur 3 depuis hier), et (b) `signals.md` est gÃ©nÃ©rÃ© chaque matin sans dÃ©pendre d'une tÃ¢che planifiÃ©e Windows fragile (3 jours consÃ©cutifs manquÃ©s).

## Ce qui a bougÃ© depuis le dernier audit

- **41 commits depuis le 25/04 11:34 (48h denses)** : v14 complet shippÃ© (S1 inference_stuck `34eb5a1`, S2 lint CI KNOWN_SECTIONS `efad1dd`, K1 hygiÃ¨ne v13 prompts `fc4e16d`), passe massive UX (Atlas thÃ¨me exposÃ© `4bd87a8`, salaire estimÃ© Jobs Radar `99140c0`, snooze d'article `2e9959f`, hero forme + sous-onglets `e165ec6`, tooltip auto-link wiki `e33d7cd`, polish Brief `51e3d85`), nouvelle feature Â« Miroir du soir Â» (`4233509` + migration `006_daily_mirror.sql`), refacto NAV en source unique (`004f55a`) qui Ã©limine le drift sidebar `data.js` â†” `data-loader.js`, fix Ã©cran blanc post-refacto (`5a8fb24`), passe a11y (contraste AA `9bb02cc`, reduced-motion `b1b0d7d` `0160f3f`).
- **Shipped depuis 2026-04-26** :
  - v14-S1 â€” `34eb5a1` : `_sync_call` dÃ©tecte `inference_stuck` aprÃ¨s 2e timeout consÃ©cutif si `check_lm_studio() == "connected"`.
  - v14-S2 â€” `efad1dd` : lint CI `lint-known-sections.yml` bloquant sur la sync `KNOWN_SECTIONS` â†” `cockpit/app.jsx`. **ConfirmÃ© en production** : commit `75d4ced fix(lint): ajoute evening Ã  KNOWN_SECTIONS` montre que le lint a immÃ©diatement attrapÃ© l'ajout du panel Miroir du soir et forcÃ© la resync. Le garde-fou structurel fonctionne.
  - v14-K1 â€” `fc4e16d` : 3 prompts v13 dÃ©placÃ©s en `done/`.
- **Friction rÃ©siduelle critique** :
  - **v14-S1 a un trou.** Sur les 4 traces post-fix (08:00 UTC le 26/04), 2 cas ont AVOIR eu attempts=3 Ã— 1114s (08:08 et 08:26), 1 cas a fired correctement (08:40 attempts=2). Cause : `check_lm_studio(timeout=2.0)` retourne `"unreachable"` (pas `"connected"`) quand LM Studio est lui-mÃªme saturÃ© au point que le `GET /v1/models` timeout. Le code passe par la branche `if attempt < MAX_RETRIES` et continue Ã  retry. **ConsÃ©quence** : 2 Ã— 1114s = 37 min consommÃ©es en silence le 26/04 matin, exactement ce que v14-S1 voulait empÃªcher.
  - **`signals.md` du 27/04 manquant** : 3e jour consÃ©cutif sans gÃ©nÃ©ration (dÃ©jÃ  manquÃ© le 25/04 et 26/04). Aucun fichier `jarvis/intel/2026-04-27-signals.md` ni `2026-04-26-signals.md`. Audit du jour fait sur `llm_traces.jsonl` + logs + git + 2 audits prÃ©cÃ©dents. P30 condition triple-remplie. Aucune automatisation visible dans le repo (pas de `.bat` pour le lancer, pas de `cron` GitHub Actions) â€” soit la Â« tÃ¢che planifiÃ©e Windows Â» mentionnÃ©e dans CLAUDE.md n'a jamais Ã©tÃ© configurÃ©e, soit elle est cassÃ©e.

## Signaux faibles (tÃ©lÃ©mÃ©trie + logs)

- **Top events 7j** (signals 25/04, dernier disponible) : `section_opened` (575), `error_shown` (154), `pipeline_triggered` (25). Sections les plus revisitÃ©es : `brief` (47), `top` (44), `radar` (30), `recos` (29), `music` (28). DonnÃ©es antÃ©rieures aux ajouts rÃ©cents (`evening`, refacto NAV) â€” la prochaine extraction sera le vrai signal.
- **Erreurs UI captÃ©es (semaine du 25/04)** : `panel:wiki` 108x (`Cannot read properties of undefined (reading 'toLocaleString')`), `panel:opps` 24x (`Cannot read properties of null (reading 'urgency')`), `panel:history` 9x. Ã‰tat inchangÃ© vs audit prÃ©cÃ©dent â€” listÃ© en P33/P35 (pas adressÃ© cette semaine).
- **`llm_traces.jsonl` (129 traces totales)** :
  - Errors par type : `APITimeoutError` 20, `model_unloaded_midrun` 2, `inference_stuck` 2, `APIConnectionError` 1.
  - Post-deploy v14-S1 (cutoff 2026-04-26T08:00 UTC) : **4 traces, dont 2 APITimeoutError Ã— 1114s Ã— attempts=3** = preuve que le filet a un trou. 1 succÃ¨s en 56s, 1 inference_stuck Ã  741s/attempts=2 = preuve qu'il fonctionne dans le cas idÃ©al.
  - Latence record rÃ©ussie : 763s/attempts=3 (chars_in 1989) â€” preuve qu'un long-prompt lÃ©gitime peut avoir besoin de >2 timeouts consÃ©cutifs. Ã€ garder en tÃªte pour le rechallenging de S1.
- **Erreurs pipelines** :
  - `last_indexer.log` : 3 erreurs 409 sur `memories_vectors` (P21 deferred â€” sous le seuil de 5/run).
  - `last_nightly_learner.log` (run du 26/04 Ã  08:27 UTC) : 4 extractions consÃ©cutives Ã©chouÃ©es en `LLM extraction failed: Request timed out.`, durÃ©e totale 4542s = **75 min de boucle de retry inutile**. Run prÃ©-deploy v14-S1 (le fix a Ã©tÃ© commitÃ© Ã  10:00 Paris = 08:00 UTC, le run a dÃ©marrÃ© vers 06:25 UTC). Ã€ recontrÃ´ler aprÃ¨s S1 mergÃ©.
  - `last_activity_brief.log` : `LLM call failed: LM Studio inference stuck on qwen/qwen3.5-9b (attempt 2/3)` â€” v14-S1 a fonctionnÃ© dans ce cas. Mais nightly_learner du 26/04 montre que le mÃªme filet a manquÃ© 2 cas sur 3.
  - `cloudflared.log` : graceful shutdown 26/04 12:56 UTC, comportement attendu.
- **MÃ©triques santÃ©** : 883 vecteurs (stable vs 26/04), 93 faits actifs, 1000 events tÃ©lÃ©mÃ©trie totaux. `status_generator.log` rapporte toujours `Chunks indexes: 0` (P34 deferred).
- **MÃ©ta-tendance audit** : v14-S1 a couvert 1/3 du pÃ©rimÃ¨tre cible. Le rechallenging du 26/04 disait Â« probabilitÃ© rÃ©elle [...] pari raisonnable que Ã§a arrive â‰¥ 1x/semaine Â». La rÃ©ponse : c'est arrivÃ© dans les 24h qui ont suivi, et le filet a manquÃ© 2/3. Confirme que la North Star de cette semaine n'est pas un raffinement gratuit mais une consolidation nÃ©cessaire.

## SHIP (max 3, aprÃ¨s rechallenging)

### S1 â€” Durcir `inference_stuck` : abort sur 2e `APITimeoutError`, peu importe l'Ã©tat `check_lm_studio()`

**Pourquoi maintenant** : sert directement la North Star. v14-S1 (`34eb5a1`) a posÃ© l'infrastructure mais l'heuristique `lm_state == "connected"` rate exactement le scÃ©nario que la North Star de la semaine prÃ©cÃ©dente voulait fermer : sous saturation VRAM totale, mÃªme `GET /v1/models` timeout, donc `check_lm_studio(timeout=2.0)` retourne `"unreachable"`, donc la branche inference_stuck ne dÃ©clenche pas, donc on retry, donc on perd 1114s Ã— 2 = 37 min en silence. ConfirmÃ© sur 2 traces post-fix le 26/04 matin (08:08, 08:26). Sans cette correction, la dette de v14-S1 reste ouverte.
**Diagnostic** : `jarvis/llm_client.py:91-130` (boucle `_sync_call`). Cf `jarvis_data/llm_traces.jsonl` 2026-04-26T08:08:11 et T08:26:45 â€” chacun `model: "qwen/qwen3.5-9b"`, `latency_ms â‰ˆ 1114000`, `attempts: 3`, `error_type: "APITimeoutError"`. La trace T08:40:06 (`error_type: inference_stuck`, attempts=2) montre que la branche fonctionne quand `check_lm_studio()` rÃ©pond â€” le bug est strict Ã  l'edge case Â« saturation + check Ã©galement stuck Â».
**DÃ©finition de "fait"** :
1. Modifier `_sync_call` : sur `APITimeoutError` Ã  `attempt >= 1`, abort immÃ©diat avec `error_type="inference_stuck"` SANS appeler `check_lm_studio()` au prÃ©alable. La branche `model_unloaded_midrun` (au 1er timeout) reste prioritaire et inchangÃ©e.
2. Sur 1 prochaine occurrence VRAM saturÃ©e vÃ©rifiÃ©e (saturation provoquÃ©e OU run nightly rÃ©el), `llm_traces.jsonl` doit montrer `attempts: 2` (pas 3), `error_type: "inference_stuck"`, et `latency_ms < 250000` (pas 1114000).
3. `last_nightly_learner.log` doit montrer `LLM call failed: LM Studio inference stuck` au lieu de `LLM extraction failed: Request timed out.` Ã— 4.
**Effort** : XS (~30 min, modification ciblÃ©e + test).

#### Prompt Claude Code â€” S1

```
Contexte projet : v14-S1 (commit 34eb5a1) a cÃ¢blÃ© la dÃ©tection inference_stuck
mais avec une condition `lm_state == "connected"` qui rate les cas de saturation
totale oÃ¹ GET /v1/models timeout aussi. ConfirmÃ© sur 2 traces post-deploy
(2026-04-26T08:08:11 et T08:26:45 dans jarvis_data/llm_traces.jsonl) :
attempts=3, latency_msâ‰ˆ1114000, error_type=APITimeoutError. Le filet a manquÃ©
2 cas sur 3 dans les 24h suivant son dÃ©ploiement.

Phase 0 â€” Reconnaissance (OBLIGATOIRE avant toute action)

Avant de modifier quoi que ce soit, Ã©cris un rapport ~25 lignes aprÃ¨s ces
vÃ©rifications :

1. Lis jarvis/llm_client.py L91-130 EN ENTIER. Tu dois distinguer 3 branches
   actuelles dans le except APITimeoutError | APIConnectionError :
   (a) `if isinstance(e, APITimeoutError) and lm_state == "no_model_loaded"` â†’
       raise model_unloaded_midrun (au 1er timeout, inchangÃ©e par ce SHIP).
   (b) `elif lm_state == "connected" and attempt >= 1` â†’ raise inference_stuck
       (la branche Ã  durcir).
   (c) Fall-through `if attempt < MAX_RETRIES` â†’ backoff + retry.

2. Lis jarvis_data/llm_traces.jsonl (les 30 derniÃ¨res lignes â€” utilise
   tail -30). Recense les traces post-cutoff 2026-04-26T08:00 UTC : tu dois
   trouver exactement :
   - 2 traces APITimeoutError attempts=3 latencyâ‰ˆ1114s (08:08, 08:26).
   - 1 trace ok attempts=1 latency=56905 (08:27).
   - 1 trace inference_stuck attempts=2 latency=741271 (08:40).
   Si diffÃ©rent, STOP et signale.

3. grep -rn "chat_completion_sync\|chat_completion_async" jarvis/ â€” vÃ©rifie
   qu'aucun appelant ne dÃ©pend du comportement Â« retry sur APITimeoutError
   Ã  attempt=2 Â» (server.py /chat, nightly_learner._llm_extract,
   daily_brief_generator, status_generator, evaluate-challenge). Pour chacun,
   verdict Â« regression-safe oui/non/Ã  investiguer Â».

4. Identifie 1 dÃ©cision design Ã  arbitrer EXPLICITEMENT :
   - Faut-il garder la branche (b) `lm_state == "connected"` comme chemin
     Â« rapide Â» qui abort dÃ¨s attempt >= 1, ET ajouter un fallback qui abort
     aussi quand lm_state != "connected" mais attempt >= 1 ?
     â†’ RecommandÃ© : OUI, fusionner. Sur APITimeoutError Ã  attempt >= 1, abort
       inconditionnel. La branche `lm_state == "no_model_loaded"` (1er timeout)
       reste prioritaire pour distinguer un unload franc d'un freeze. La
       branche `lm_state == "connected"` devient redondante, on la supprime.

5. ConsidÃ¨re le cas lÃ©gitime Â« long prompt 763s succÃ¨s au 3e attempt Â»
   (trace 2026-04-25T14:27:05 chars_in=1989 tokens_out=1101 attempts=3
   status=ok). Ce cas DEVIENDRA un faux positif aprÃ¨s ce SHIP : on aurait
   abort en attempt=2. Verdict : compromis acceptable car (i) 1/129 traces,
   (ii) les long-prompts >120s sont dÃ©jÃ  rares et hors flux normal, (iii) le
   coÃ»t d'attendre 1114s en silence > coÃ»t d'aborter et de relancer un
   long-prompt en mode dÃ©gradÃ©. Documente Ã§a dans le commentaire du code.

ATTENDS validation explicite avant de coder.

Objectif : Sur APITimeoutError Ã  attempt >= 1, abort en â‰¤ 250s avec trace
inference_stuck â€” sans dÃ©pendre de la disponibilitÃ© de check_lm_studio().

Fichiers concernÃ©s :
- jarvis/llm_client.py (modification ciblÃ©e du except â€” 1 fichier, 1 fonction).

Ã‰tapes (aprÃ¨s validation) :

1. Dans jarvis/llm_client.py, dans le bloc except APITimeoutError | APIConnectionError
   de _sync_call, restructurer comme suit :

   ```python
   except (APITimeoutError, APIConnectionError) as e:
       last_exc = e
       if isinstance(e, APITimeoutError):
           # 1er timeout : check si modÃ¨le dÃ©chargÃ© (raise immÃ©diat)
           if attempt == 0:
               lm_state = check_lm_studio(timeout=2.0)
               if lm_state == "no_model_loaded":
                   _write_trace({
                       "ts": datetime.now(timezone.utc).isoformat(),
                       "model": model_used,
                       "chars_in": chars_in,
                       "latency_ms": round((time.time() - start_time) * 1000),
                       "attempts": attempt + 1,
                       "status": "error",
                       "error_type": "model_unloaded_midrun",
                   })
                   raise RuntimeError(
                       f"LM Studio unloaded model {model_used} mid-run "
                       f"(after attempt {attempt + 1})"
                   ) from e
           # 2e timeout (ou plus) : inference probablement stuck
           # (VRAM saturÃ©e, check_lm_studio peut Ãªtre lui-mÃªme injoignable).
           # Compromis assumÃ© : on abandonne aussi quelques long-prompts
           # lÃ©gitimes >120s Ã— 2 â€” ils sont rares (1/129 historiquement)
           # et coÃ»tent moins que 1114s de silence.
           else:
               _write_trace({
                   "ts": datetime.now(timezone.utc).isoformat(),
                   "model": model_used,
                   "chars_in": chars_in,
                   "latency_ms": round((time.time() - start_time) * 1000),
                   "attempts": attempt + 1,
                   "status": "error",
                   "error_type": "inference_stuck",
               })
               raise RuntimeError(
                   f"LM Studio inference stuck on {model_used} "
                   f"(attempt {attempt + 1}/{MAX_RETRIES + 1})"
               ) from e
       if attempt < MAX_RETRIES:
           backoff = 2 ** (attempt + 1)
           log.warning("LLM retry %d/%d after %s (backoff %ds)",
                       attempt + 1, MAX_RETRIES, type(e).__name__, backoff)
           time.sleep(backoff)
   ```

   Note : APIConnectionError reste retry-friendly (rÃ©seau flake â‰  inference
   stuck). Seul APITimeoutError dÃ©clenche l'abort Ã  attempt >= 1.

2. Validation reproductible :
   - python -m py_compile jarvis/llm_client.py jarvis/nightly_learner.py jarvis/server.py
     â†’ exit 0
   - Test mock (sans LM Studio) : ajouter un test ad-hoc dans test_jarvis.py
     OU script jetable :
     ```python
     from unittest.mock import patch, MagicMock
     from openai import APITimeoutError
     from jarvis.llm_client import _sync_call

     mock_client = MagicMock()
     mock_client.chat.completions.create.side_effect = APITimeoutError("stuck")
     with patch("jarvis.llm_client.get_client", return_value=mock_client):
         try:
             _sync_call([{"role":"user","content":"x"}], 10, 0.0)
         except RuntimeError as e:
             print("OK", e)
     ```
     â†’ doit afficher `OK LM Studio inference stuck on ...` (PAS APITimeoutError).
   - tail -1 jarvis_data/llm_traces.jsonl â†’ `error_type: "inference_stuck"`,
     `attempts: 2`.

3. VÃ©rifier la durÃ©e wallclock : le test mock doit retourner en
   < 10s (1 timeout simulÃ© instantanÃ© + backoff 2s + 2e timeout simulÃ©).
   Sur un vrai timeout de 120s Ã— 2 + backoff 2s = 242s max â€” bien < 1114s.

Contraintes :
- Ne touche PAS Ã  la branche APIConnectionError fall-through (rÃ©seau retry
  reste utile).
- Ne change PAS la signature publique de chat_completion_sync /
  chat_completion_async ni la valeur MAX_RETRIES.
- Ne supprime PAS check_lm_studio() â€” la branche `attempt == 0 + no_model_loaded`
  l'utilise toujours.
- Ne touche PAS aux call sites (server.py, nightly_learner, status_generator,
  daily_brief_generator) â€” ils captent dÃ©jÃ  RuntimeError si nÃ©cessaire.
- Pas de nouvelle variable d'env, pas de nouvelle dÃ©pendance.

Validation :
- python -m py_compile jarvis/llm_client.py â†’ exit 0.
- grep -c "inference_stuck" jarvis/llm_client.py â†’ â‰¥ 2 (1 dans le trace, 1
  dans le RuntimeError message).
- grep -c "lm_state == \"connected\"" jarvis/llm_client.py â†’ 0 (ancienne
  condition supprimÃ©e).
- Test mock APITimeoutError Ã— 2 â†’ RuntimeError "stuck" en attempt=2.
- python -m py_compile $(grep -lE "chat_completion" jarvis/*.py) â†’ tous OK.

Ne fais PAS :
- N'introduis pas de circuit breaker stateful inter-runs.
- N'ajoute pas de notification email/Slack/push.
- Ne refactore pas _sync_call au-delÃ  du bloc except citÃ©.
- Ne touche pas au prÃ©flight nightly_learner.
- Ne touche pas au cockpit (panel-jarvis.jsx etc.).
- Ne push pas.

Quand c'est fait : montre-moi le diff complet AVANT git add + le rÃ©sultat
du test mock. git commit avec message "fix(jarvis): inference_stuck â€”
abort au 2e timeout sans dÃ©pendre de check_lm_studio".
PAS de push.
```

#### Rechallenging â€” S1
- Red team : Â« Tu sacrifies les long-prompts lÃ©gitimes (1989 chars in, succÃ¨s au 3e attempt en 763s â€” vu le 25/04). AprÃ¨s ce SHIP, ce cas devient un faux positif. Â» RÃ©futation : 1 cas sur 129 traces (0.8%), coÃ»t abortÃ© = 0 (Jean relance ou raccourcit le prompt). CoÃ»t NON-abortÃ© = 1114s Ã— 2 en silence (plusieurs fois constatÃ©). Le compromis penche fort vers l'abort. DocumentÃ© dans le code.
- Contre-exemple : si un futur modÃ¨le plus rapide rend les long-prompts >120s plus frÃ©quents (genre on revient sur thinking-mode), on aborte des appels qui auraient abouti. RÃ©futation : ce SHIP est traÃ§able via `error_type: "inference_stuck"` â€” si on observe >5 inference_stuck/semaine sur des prompts de chars_in modÃ©rÃ©s (<3000), on saura que le seuil est trop agressif et on rÃ©-Ã©valuera (rÃ©veil PARK conditionnÃ©).
- CoÃ»t d'opportunitÃ© : 30 min vs (a) signals.md fix (S2, S effort, sert aussi NS), (b) wiki TypeError (XS, hors NS), (c) Chunks: 0 status_generator (XS, hors NS). S1 + S2 sont les 2 piliers de la NS â€” capacitÃ©s complÃ©mentaires, pas concurrentes.
- Futur soi : Â« J'ai sur-corrigÃ© un edge case observÃ© 2 fois et cassÃ© le retry pour des cas lÃ©gitimes. Â» ProbabilitÃ© faible â€” la trace `inference_stuck` est observable, donc rÃ©gression visible immÃ©diatement.
- CohÃ©rence historique : v14-S1 (`34eb5a1`) a posÃ© l'infrastructure inference_stuck. Ce SHIP la consolide, ne la contredit pas. RAS.
- **Verdict final : MAINTENU**

### S2 â€” Coupler la gÃ©nÃ©ration de `signals.md` au dÃ©marrage Jarvis (`run_nightly_after_deps.bat`)

**Pourquoi maintenant** : sert directement la North Star. 3 jours consÃ©cutifs sans `signals.md` (25/04, 26/04, 27/04). Le repo n'a aucun script `.bat` ni aucune Action GitHub qui lance `extract_signals.py` â€” la Â« tÃ¢che planifiÃ©e Windows Â» mentionnÃ©e dans CLAUDE.md est soit non configurÃ©e, soit cassÃ©e silencieusement. P30 condition triple-remplie. Tant que ce trou existe, le diagnostic hebdo est dÃ©gradÃ© (cet audit-ci en est la preuve : fait sans signals.md du jour, sur des fichiers logs + traces seulement).
**Diagnostic** : `jarvis/run_nightly_after_deps.bat` exÃ©cute dÃ©jÃ  `nightly_learner.py` puis `daily_brief_generator` â€” c'est le bon point d'ancrage pour ajouter `extract_signals.py`. Coupler la gÃ©nÃ©ration Ã  une action que Jean fait tous les jours (lancer Jarvis) > rester sur une tÃ¢che fantÃ´me. Le script `extract_signals.py` est idempotent (Ã©crit dans `jarvis/intel/YYYY-MM-DD-signals.md`, durÃ©e ~3s d'aprÃ¨s les anciens runs).
**DÃ©finition de "fait"** :
1. `jarvis/run_nightly_after_deps.bat` exÃ©cute `python jarvis/scripts/extract_signals.py > jarvis_data/last_extract_signals.log 2>&1` aprÃ¨s nightly_learner et activity_brief.
2. Sur le prochain `start_jarvis.bat`, `jarvis/intel/YYYY-MM-DD-signals.md` est Ã©crit dans la journÃ©e (avec la date du jour).
3. Si le fichier du jour existe dÃ©jÃ , le run le re-Ã©crase (comportement actuel du script â€” pas de garde idempotent additionnel Ã  ajouter).
4. Documentation CLAUDE.md mise Ã  jour pour reflÃ©ter la rÃ©alitÃ© : Â« le pipeline Ã©tape 1 (signals) est lancÃ© au dÃ©marrage de Jarvis, pas par une tÃ¢che planifiÃ©e Windows sÃ©parÃ©e Â».
**Effort** : XS (~30-45 min : 1 ligne de batch + 1 paragraphe doc + 1 vÃ©rif manuelle au prochain start).

#### Prompt Claude Code â€” S2

```
Contexte projet : signals.md du 25/04, 26/04, 27/04 manquent (3 jours
consÃ©cutifs). Aucun .bat ni .yml dans le repo ne lance jarvis/scripts/
extract_signals.py. CLAUDE.md mentionne Â« tÃ¢che planifiÃ©e Windows Ã  05h30 UTC Â»
mais elle est invisible dans le repo donc soit jamais configurÃ©e soit cassÃ©e.
La cible : coupler la gÃ©nÃ©ration de signals.md Ã  start_jarvis.bat (action
quotidienne fiable de Jean) plutÃ´t que dÃ©pendre de Task Scheduler.

Phase 0 â€” Reconnaissance (OBLIGATOIRE avant toute action)

1. Lis jarvis/run_nightly_after_deps.bat EN ENTIER (~20 lignes).
2. Lis jarvis/start_jarvis.bat L40-95 (la chaÃ®ne d'orchestration : indexer â†’
   status â†’ nightly â†’ tunnel â†’ server).
3. Lis jarvis/scripts/extract_signals.py L1-80 (vÃ©rifie : entrÃ©e standalone
   `if __name__ == "__main__"`, sortie dans `jarvis/intel/YYYY-MM-DD-signals.md`,
   pas d'env var critique au-delÃ  de SUPABASE_URL / SUPABASE_KEY dÃ©jÃ 
   exportÃ©es par start_jarvis.bat, pas de dÃ©pendance pgvector cÃ´tÃ© Ã©criture).
4. ls jarvis/intel/ | grep signals | tail -5 â†’ confirme : derniers signals
   datÃ©s 23/04 et 25/04.
5. grep -rn "extract_signals" .github/workflows/ jarvis/ | head â€” vÃ©rifier
   qu'aucune autre orchestration ne le lance dÃ©jÃ  (sinon double-run Ã  Ã©viter).
6. Identifie 1 dÃ©cision design :
   - Lancer extract_signals AVANT ou APRÃˆS nightly_learner ?
     â†’ RecommandÃ© : APRÃˆS, car nightly_learner peut produire des `usage_events`
       indirects via les traces (limite : peu probable). Plus simple : le
       lancer en parallÃ¨le de daily_brief en fin de chain (run_nightly_after_deps).

Ã‰cris un rapport ~15 lignes :
- Diff prÃ©vu sur run_nightly_after_deps.bat (ajout de 3 lignes : echo + run +
  echo).
- Liste les 8 sections existantes que extract_signals.py va lire dans
  Supabase (articles, ...) â€” confirme via le code qu'elles sont dÃ©jÃ  accessibles
  avec les env vars actuelles.
- DÃ©cision design + justification (avant/aprÃ¨s nightly).
- Note CLAUDE.md Ã  mettre Ã  jour (section Â« Calendrier Â» du Weekly Pipeline) â€”
  passer de Â« 05h30 / Ã‰tape 1 / Python / tÃ¢che planifiÃ©e Â» Ã  Â« au dÃ©marrage
  start_jarvis.bat / via run_nightly_after_deps.bat Â».

ATTENDS validation explicite.

Objectif : Garantir qu'Ã  chaque dÃ©marrage de Jarvis, signals.md du jour est
gÃ©nÃ©rÃ© (ou re-gÃ©nÃ©rÃ© si dÃ©jÃ  lÃ ).

Fichiers concernÃ©s :
- jarvis/run_nightly_after_deps.bat (modification â€” ajout d'un bloc).
- CLAUDE.md (section Â« Calendrier Â» du Weekly Pipeline + section Â« Lancement
  manuel Â» â€” alignement doc/code).
- jarvis_data/.gitignore ou jarvis_data/README.md si nÃ©cessaire pour
  documenter `last_extract_signals.log` (probablement dÃ©jÃ  couvert par le
  gitignore global de jarvis_data/).

Ã‰tapes (aprÃ¨s validation) :

1. Modifier jarvis/run_nightly_after_deps.bat pour ajouter, aprÃ¨s le bloc
   activity_brief :
   ```bat
   echo [%date% %time%] Extraction signaux internes demarre...
   python jarvis\scripts\extract_signals.py > jarvis_data\last_extract_signals.log 2>&1
   echo [%date% %time%] Extraction signaux terminee (code=%errorlevel%)
   ```

2. Mettre Ã  jour CLAUDE.md :
   - Section Â« Weekly Pipeline / Calendrier Â» : remplacer la ligne
     Â« 05h30 / 1 / Python / ... / signals.md Â» par
     Â« Au dÃ©marrage Jarvis / 1 / Python via run_nightly_after_deps.bat / ... /
       signals.md Â».
   - Section Â« Lancement manuel Â» : complÃ©ter avec
     `python jarvis/scripts/extract_signals.py` (dÃ©jÃ  mentionnÃ© OK,
     vÃ©rifier que c'est cohÃ©rent).
   - Section Â« Marges et fail-safe Â» : la marge 30 min entre Ã©tape 1 et 2
     n'a plus de sens (on quitte le mode sÃ©quentiel par cron) â€” soit la
     supprimer, soit la reformuler pour la pipeline Cowork (Ã©tapes 2+3
     restent sur Task Scheduler).
   - Note de transition : prÃ©ciser qu'Ã©tapes 2 et 3 (Cowork audit + veille)
     restent sur Task Scheduler / Cowork interne ET dÃ©pendent de la
     fraÃ®cheur de signals.md du jour (donc de start_jarvis.bat ayant tournÃ©).

Contraintes :
- Ne crÃ©e PAS de nouveau .bat sÃ©parÃ© â€” rÃ©utilise run_nightly_after_deps.bat
  pour ne pas multiplier les points d'ancrage.
- Ne lance PAS extract_signals.py dans la boucle wait_loop â€” c'est un appel
  ponctuel aprÃ¨s nightly + brief.
- Ne touche PAS Ã  start_jarvis.bat (le wrapper fait dÃ©jÃ 
  start /B "" jarvis\run_nightly_after_deps.bat â€” la modif est invisible
  cÃ´tÃ© start).
- Ne configure PAS de Task Scheduler depuis le code Claude â€” Jean l'a dÃ©jÃ 
  ou pas, ce SHIP rend l'orchestration Task Scheduler optionnelle.
- N'utilise PAS d'argument CLI Ã  extract_signals.py â€” il consomme TODAY
  via datetime.now(), c'est volontaire.

Validation :
- python -m py_compile jarvis/scripts/extract_signals.py â†’ exit 0 (sanity).
- python jarvis/scripts/extract_signals.py â€” run direct local Windows
  (cd C:\Users\johnb\projects\jarvis-cockpit, env vars exportÃ©es) â†’ produit
  jarvis/intel/2026-04-27-signals.md en < 10s.
- ls -la jarvis/intel/2026-04-27-signals.md â†’ fichier existe + taille > 0.
- Au prochain start_jarvis.bat : tail -5 jarvis_data/last_extract_signals.log
  â†’ message Â« Extraction signaux terminee (code=0) Â».
- Au prochain start_jarvis.bat : ls jarvis/intel/ | grep $(date +%Y-%m-%d)
  â†’ fichier signals du jour prÃ©sent.

Ne fais PAS :
- N'ajoute pas de timeout explicite au run extract_signals.py â€” il finit en 3s
  d'habitude.
- N'ajoute pas de retry automatique en cas d'Ã©chec â€” un Ã©chec doit Ãªtre visible
  dans last_extract_signals.log pour diagnostic ultÃ©rieur.
- Ne lance pas extract_signals au PREMIER plan (sans `start /B`) â€” sinon
  Jean attend pour rien.
- Ne push pas.

Quand c'est fait : montre-moi le diff complet de run_nightly_after_deps.bat +
le diff CLAUDE.md AVANT git add. Lance manuellement extract_signals.py une fois
(commande indiquÃ©e ci-dessus) + montre le contenu du signals.md gÃ©nÃ©rÃ© (head -30).
git commit avec message "feat(jarvis): coupler signals.md au dÃ©marrage Jarvis
(supprime dÃ©pendance Task Scheduler fragile)".
PAS de push.
```

#### Rechallenging â€” S2
- Red team : Â« Tu dÃ©places le problÃ¨me â€” si Jean ne lance pas Jarvis un jour, signals.md ne se gÃ©nÃ¨re pas. La Task Scheduler aurait dÃ» marcher. Â» RÃ©futation : (i) Task Scheduler a Ã©chouÃ© 3 fois consÃ©cutives sans cause identifiÃ©e â€” la cause n'est pas dans le repo, donc nul moyen pour Claude Code de la fixer. (ii) Jean lance Jarvis quotidiennement (pattern observÃ© via cloudflared.log timestamps + activity_*.jsonl quasi-quotidiens). Coupler Ã  une action dÃ©jÃ  fiable > continuer Ã  parier sur une infra qu'on ne peut pas inspecter.
- Contre-exemple : un dimanche off, Jean ne lance pas Jarvis, audit du lundi tourne sans signals du dimanche. RÃ©futation : c'est dÃ©jÃ  l'Ã©tat actuel, sans la garantie d'avoir signals.md sur les jours Â« ON Â». Au minimum, le SHIP redonne 5 jours/7 fiables au lieu de 0/7.
- CoÃ»t d'opportunitÃ© : 30 min vs (a) wiki TypeError (XS, hors NS), (b) status_generator Chunks: 0 (XS, hors NS), (c) cloud fallback nightly (S, redondant avec S1). S2 est le seul qui ferme le 2e pilier de la NS.
- Futur soi : Â« J'aurais dÃ» investiguer la vraie cause de la Task Scheduler cassÃ©e, pas la bypass. Â» Faible : l'investigation est non-bornÃ©e (env vars, paths, Python install Windows-side, droits) et la solution batch est strictement plus simple ET vÃ©rifiable. Dans 3 mois, le SHIP rend explicite et auditable ce qui Ã©tait implicite et fragile.
- CohÃ©rence historique : P30 (signals.md du 25/04 manquant) â†’ diffÃ©rÃ© pour Â« 2e jour consÃ©cutif Â». Aujourd'hui c'est le 3e jour consÃ©cutif. Promotion lÃ©gitime, pas un revirement.
- **Verdict final : MAINTENU**

## SHIP rÃ©trogradÃ©s pendant le rechallenging

Aucun ce tour-ci. Justification (mÃ©ta-test) : 2 SHIP seulement, tous deux directement et explicitement sourcÃ©s sur des traces ou logs prÃ©cis (`llm_traces.jsonl` post-deploy + `jarvis/intel/` 3 jours manquants). Pas de 3e SHIP candidat Ã  rÃ©trograder pour Â« forcer la cible 20% Â» â€” j'ai prÃ©fÃ©rÃ© 2 SHIP solides Ã  3 dont 1 fragile. Le ratio cumulÃ© reste sain (18.8% via INDEX). Si dans les prochains audits aucune rÃ©trogradation n'arrive, je forcerai. Pour cette semaine, le rechallenging a posÃ© des objections vraies (long-prompts lÃ©gitimes, Jean off un jour) que les 2 SHIP intÃ¨grent dans leur compromis assumÃ©.

## KILL list

### K1 â€” HygiÃ¨ne : dÃ©placer les 3 prompts v14 (S1, S2, K1) `pending/` â†’ `done/` + actualiser `prompts/README.md`

**Raison** : Les 3 prompts v14 sont shippÃ©s (commits `34eb5a1`, `efad1dd`, `fc4e16d`) mais traÃ®nent encore dans `jarvis/upgrades/prompts/pending/`. MÃªme pattern que v14-K1 lui-mÃªme il y a 24h pour v13. Le `prompts/README.md` ne reflÃ¨te plus la rÃ©alitÃ© du dossier â€” bruit cognitif Ã  chaque ouverture. Sans cette KILL, le pattern Â« v14-K1 a corrigÃ© ses propres ancÃªtres mais pas lui-mÃªme Â» se reproduira encore au v15.
**Risque de regret** : Faible â€” les fichiers vivent dans `done/`, ils sont juste classÃ©s. Le README est un Ã©tat instantanÃ© Â« Ã  faire Â».
**Rechallenging court** :
- Test 1 (red team) : Â« Pourquoi pas automatiser ce mÃ©nage pour de bon ? Â» RÃ©futation : automatiser ferait sens si la cadence Ã©tait >5/semaine. Ã€ 3 prompts/semaine, le coÃ»t manuel = 5 min, le coÃ»t d'Ã©crire et tester un script de classement automatique = 1h. ROI inversÃ©. Ã€ reconsidÃ©rer si la cadence d'audits triple.
- Test 4 (futur soi) : Â« J'ai cassÃ© une recherche future. Â» Aucun risque â€” `done/` est l'historique normal, accessible via `ls`/`grep`.

**Prompt Claude Code** :

```
Phase 0 â€” VÃ©rification :
1. ls jarvis/upgrades/prompts/pending/ â†’ tu dois voir EXACTEMENT ces 3 fichiers :
   2026-04-26-K1-hygiene-v13-prompts.md
   2026-04-26-S1-inference-stuck-detection.md
   2026-04-26-S2-lint-known-sections.md
2. git log --oneline --grep="inference_stuck" â†’ doit trouver 34eb5a1.
3. git log --oneline --grep="lint bloquant KNOWN_SECTIONS" â†’ doit trouver efad1dd.
4. git log --oneline --grep="hygiÃ¨ne â€” v13 prompts" â†’ doit trouver fc4e16d.
5. ls jarvis/upgrades/prompts/done/ | grep "2026-04-26" â†’ vÃ©rifier qu'aucun
   homonyme n'existe (sinon renommer en -v2.md).

Si OK :
6. git mv jarvis/upgrades/prompts/pending/2026-04-26-K1-hygiene-v13-prompts.md \
        jarvis/upgrades/prompts/done/2026-04-26-K1-hygiene-v13-prompts.md
7. git mv jarvis/upgrades/prompts/pending/2026-04-26-S1-inference-stuck-detection.md \
        jarvis/upgrades/prompts/done/2026-04-26-S1-inference-stuck-detection.md
8. git mv jarvis/upgrades/prompts/pending/2026-04-26-S2-lint-known-sections.md \
        jarvis/upgrades/prompts/done/2026-04-26-S2-lint-known-sections.md

9. RÃ©Ã©cris jarvis/upgrades/prompts/README.md pour reflÃ©ter le nouvel Ã©tat
   (les 3 prompts du 2026-04-27 â€” S1 inference_stuck v2 + S2 signals.md +
   K1 hygiÃ¨ne v14 â€” crÃ©Ã©s par l'audit du jour) :

   ```markdown
   # Prompts Claude Code en attente

   | Fichier | Titre | Effort | Date audit |
   |---------|-------|--------|------------|
   | [v15-S1](pending/2026-04-27-S1-inference-stuck-tighten.md) | Durcir inference_stuck (abort sans check_lm_studio) | XS | 2026-04-27 |
   | [v15-S2](pending/2026-04-27-S2-signals-on-startup.md) | Coupler signals.md au dÃ©marrage Jarvis | XS | 2026-04-27 |
   | [v15-K1](pending/2026-04-27-K1-hygiene-v14-prompts.md) | HygiÃ¨ne : v14 prompts pending â†’ done | XS | 2026-04-27 |

   **Workflow** : copier-coller le contenu d'un fichier dans Claude Code,
   exÃ©cuter, revoir le diff, commit, puis dÃ©placer le fichier vers `done/`.
   Pour cette itÃ©ration, exÃ©cuter K1 EN PREMIER (5 min, range les v14)
   pour ne pas mÃ©langer les Ã©tats.
   ```

git commit avec message "chore(jarvis): hygiÃ¨ne â€” v14 prompts pending â†’ done +
refresh prompts README".
PAS de push.
```

## PARK

| IdÃ©e | Condition de rÃ©veil |
|------|---------------------|
| P27 â€” Bump `project_status.yaml` avec Phase 7 mÃ©ta-doc | Quand les 4 lints CI Phase 7 tournent 7j sans warning ni intervention manuelle |
| P28 â€” Hybrid search BM25 + vector + RRF | Maintenant que vectors=883 (â‰¥500) ET 1 SHIP M de capacitÃ© disponible la semaine â€” semaine prochaine si pas d'incident urgent |
| P29 â€” Mem0g graph extension | Quand P28 shippÃ© ET â‰¥ 3 cas de contradiction de faits observÃ©s dans `profile_facts` en 1 semaine |
| P30 â€” Investiguer pourquoi `signals.md` Task Scheduler ne tourne pas | **AbsorbÃ© par S2-v15** â€” la Task Scheduler devient optionnelle. Si Jean a un usage oÃ¹ Jarvis ne tourne pas certains jours et qu'il veut des signals quand mÃªme, rÃ©-investiguer la Task Scheduler. |
| P32 â€” Cloud fallback dans `nightly_learner._llm_extract` aprÃ¨s Ã©chec local | Si aprÃ¨s 2 semaines avec S1 mergÃ©, â‰¥ 3 nuits consÃ©cutives sans extraction malgrÃ© abort propre |
| P33 â€” Fix wiki TypeError sur `e.word_count.toLocaleString` (108 occurrences) | Prochaine fenÃªtre oÃ¹ North Star est UX-focused, OU > 200 occurrences/semaine, OU Jean rapporte une frustration explicite |
| P34 â€” Fix `Chunks: 0` bug dans `status_generator.get_chunks_count()` | Quand snapshot Jarvis Lab consultÃ© â‰¥ 3x/semaine OU feature dÃ©pend de cette mÃ©trique |
| P35 â€” Fix `panel:opps` TypeError null.urgency (24 occurrences) | Prochaine fenÃªtre UX-focused (cogroupÃ© avec P33) |

## Edge â€” Ce qu'on pique cette semaine

**Rien Ã  voler cette semaine.** Recherche sur Â« LM Studio VRAM saturation watchdog timeout detection Â» : la doc 2026 traite VRAM management cÃ´tÃ© config (% offload, KV cache), pas de pattern publiÃ© pour dÃ©tecter la saturation cÃ´tÃ© client. Le pattern proposÃ© (abort sur 2e timeout consÃ©cutif sans dÃ©pendre d'une route /health du serveur local) est minimal et propre â€” pas de ressource externe Ã  imiter. Les Mem0g, hybrid search RRF, et Instructor restent en backlog (P28, P29, P23).

## Tendance

Jarvis va **mieux qu'il y a 7 jours** mais avec une **dette de fiabilitÃ© ouverte cette semaine**. v14 a livrÃ© 3/3 (KNOWN_SECTIONS lint CI prouvÃ© en production via le commit `evening` 24h plus tard, model_unloaded_midrun v13 + inference_stuck v14). La passe UX shell + Miroir du soir + refacto NAV = front solide, ergonomie sensiblement meilleure. Mais le filet inference_stuck a un trou (2 cas/3 ratÃ©s le 26/04) et signals.md n'est plus gÃ©nÃ©rÃ© (3 jours consÃ©cutifs). Les 2 SHIP de cette semaine sont des consolidations directes â€” pas des nouveaux chantiers â€” qui ferment prÃ©cisÃ©ment les trous laissÃ©s par v14 et P30. Le rythme de SHIPs Â« petits, ciblÃ©s, traÃ§ables Â» continue Ã  dominer. Backlog PARK monte Ã  36 entrÃ©es (P35 ajoutÃ©) â€” Ã©lagage Ã  prÃ©voir d'ici 2-3 semaines.

## MÃ©ta-tendance (mensuelle, Ã  remplir le 1er audit du mois)

*Pas premier audit du mois â€” section non remplie cette semaine. Prochain check : audit du 2026-05-01 ou suivant.*

---

## DerniÃ¨re MAJ

- **2026-04-27** : audit v15. 2 SHIP (durcir inference_stuck en supprimant la dÃ©pendance Ã  check_lm_studio + coupler signals.md Ã  start_jarvis.bat), 1 KILL (hygiÃ¨ne v14 prompts), 0 SHIP rÃ©trogradÃ© (justifiÃ© dans le mÃ©ta-test : 2 SHIP solides plutÃ´t que 3 dont 1 fragile), 1 PARK ajoutÃ© (P35 panel:opps null.urgency). P30 absorbÃ© par S2.
